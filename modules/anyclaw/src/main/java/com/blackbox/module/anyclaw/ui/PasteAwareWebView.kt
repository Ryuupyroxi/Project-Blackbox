package com.blackbox.module.anyclaw.ui

import android.content.Context
import android.util.AttributeSet
import android.webkit.WebView

class PasteAwareWebView(context: Context, attrs: AttributeSet? = null) : WebView(context, attrs) {
    fun injectImageFromUri(uri: android.net.Uri) {
        // Image paste injection stub.
    }
}
